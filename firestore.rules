
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    match /users/{userId} {
      // Leitura: O próprio usuário ou Admin
      allow get: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      
      // Listagem: Apenas Admin
      allow list: if isAdmin();
      
      // Criação: Admin pode criar qualquer um. 
      // Usuário comum só pode criar o próprio (bootstrap) se for role 'user'.
      allow create: if isAdmin() || (
        isSignedIn() && 
        request.auth.uid == userId && 
        request.resource.data.role == 'user'
      );
      
      // Atualização: Admin tem controle total. 
      // Usuário só pode atualizar o próprio se NÃO mudar o role.
      allow update: if isAdmin() || (
        isSignedIn() && 
        request.auth.uid == userId && 
        request.resource.data.role == resource.data.role
      );
      
      // Deleção: Apenas Admin
      allow delete: if isAdmin();
    }

    match /system/config {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /{path=**} {
      allow read, write: if false;
    }
  }
}
